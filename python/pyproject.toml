[project]
name = "advent_of_code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
]

[dependency-groups]
dev = [
    "ipynb>=0.5.1",
]
