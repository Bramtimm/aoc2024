struct Matrix<T> {
    ncols: usize,
    nrows: usize,
    data: Vec<T>,
}

// impl<T> Matrix<T> {
//     fn new(rows: usize, cols: usize, initial: T) -> Self
//     where T: Clone {

//     }
// }

pub fn advent_of_code4a(input_str: &str) -> i32 {
    18
}
